{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "marketing.fullname" . }}
  labels:
    {{- include "marketing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "marketing.selectorLabels" . | nindent 4 }}
{{- end }}
